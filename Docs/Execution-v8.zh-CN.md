# v8 审计后续工作记录

以 2026-09-18 审计方案的 V8-00 → V8-01 开始，沿用 UE 官方模型和基础几何体。后续包不因本文件存在而视为完成。

## V8-00 可复现基线

- `Docs/Tests-v8.json` 从实际注册入口整理小规则、短场景、网络及未执行渲染/打包类别；规模用例默认排除。
- `Scripts/VerifyV8.ps1` 导出提交 SHA、启动时脏状态、输入文件哈希、引擎构建、目标、命令、夹具版本、逐项结果与本地证据目录。产物在 Saved，不提交日志、机器信息或个人存档。
- `Docs/RX-v8.json` 保留 RX-01～50 的原始场景与期望，给出相关检查/代码线索，所有完整 RX 验收保持 NotRun。部分性质用例通过不等于完整 RX 通过。
- 合成夹具 `Tests/AetherLegacyFixture.h/.cpp` 冻结提交 38d5839 的 v6 记录字段（没有 MaterialSchema），使用 UE tagged-property 编码后读入当前结构。固定绳材料、雨水、焓和完整度，不从当前 Capture 倒推旧数据。小测试检查默认 schema0 和水的电资格；短场景替换隔离世界的绳记录，真正 Restore 后 Capture 为 schema1。
- 夹具覆盖旧记录布局，不声称覆盖旧引擎完整 .sav 容器或任意古老存档；无需本机既有个人存档。
- `TestV8NetworkBaseline.ps1` 仅启动一次编辑器独立服务器和两个 NullRHI 客户端：重复领取不增发、各自 ASC 所有权、共享供水标记、客户端不运行权威反应模拟。它不证明跨玩家隐私、持物竞争、晚加入全状态、重连或监听主机行为。

## 待完成工作包

V8-01 服务事务、V8-02 物品/任务定义、V8-03 GAS 动作生命周期、V8-04 接触/受电契约、V8-05 导航闭环、V8-06 两场景复用与可读性、V8-07 针对性双人验收仍需各自记录实现和证据。

实际结果见 `Verification-v8-baseline.json`：43 小规则、16 短场景断言和一次双客户端基线通过。首次聚合发现旧脚本传递空 `-AetherLegacyFixture=` 会将下个参数当值；已改为仅有值时传递并单独复跑场景。基础提交 0ec60b0 的原始 42 小规则也已重跑通过。报告明确标记工作树测试，不伪称在干净新 SHA 上测试。
