# v10 供水与电源服务

供水和电源使用原生 ExecuteInteraction 的固定字节命令、目标实例版本及角色/世界版本联合事务。开关的目标值来自服务器动作定义，客户端不能提交任意机制数据。供水现场条件、接收功率和事实来源由服务器核实，任务结算与世界标志同批确认。

发布只应用被事务改变的电源布尔值，不重放提交前捕获的能量、位置或质量。分区冻结记录同步该布尔值；检查点采集先发布其读取到的最新已提交版本，避免回调尚未轮询时覆盖新开关。

状态：仅完成源码和数据，未编译、未测试；不代表完整 v10 或运行时验收通过。
