# 正式 Designer 布局
新增有界的 Editor WidgetTree 作者工具和 WidgetLayouts.json：菜单根栈、菜单壳、六页正文、对话、背包格子、装备槽、技能节点、详情、数量/确认、角色预览及 HUD 区域都有实际树描述。作者工具按子蓝图依赖顺序生成并保存可在 UMG Designer 编辑的树，确认 Blueprint 编译状态；不再只制作空的原生父类子蓝图。

原生页面按同名、类型相符的字段绑定控件，单独绑定行为；动态格子、任务行、成员列表和 HUD 数值仍消费实时快照。Designer 按钮完整接入已有操作、焦点和模态会话。主题、图标和预览材质保持统一作者入口。静态布局不会执行任何游戏命令。

作者状态：代码和布局描述已写入，尚未运行 UE 作者脚本。未编译、未测试；资源生成和视觉/输入校验必须在统一阶段实际完成。
