# v10 CommonUI 和独立页面源码

本单元未编译、未测试；正式 WBP/图标生成与分辨率、键鼠/手柄验收尚未执行。

- CommonUI 已启用，平台 Engine 配置选择 CommonGameViewportClient，Server 配置显式使用引擎基础 ViewportClient。没有编辑个人改动的 DefaultEngine.ini。
- LocalPlayer 菜单子系统保留页码、选择记忆和精确模态令牌；鼠标/输入模式交给 CommonUI 激活树。根节点包含游戏输入、主菜单栈、独立确认栈。
- 菜单壳显示背包、技能、任务、地图、队伍、设置六个独立页面。对话进入同一主栈。确认框进入根模态栈，跨页/Pawn/会话失效时按令牌移除。
- 任务日志从原生快照绘制任务/目标卡片和待领奖励集合。奖励现在原子领取当前可容纳部分，剩余物品保留原 RewardId，全部清空后才记录领取完成。
- 地图使用规范区域底图与稳定服务/据点、玩家朝向、队友和任务锚点，支持缩放、平移、筛选和选点。传送只发据点 ID/档案版本，服务器复验解锁与安全状态，再走现有就绪流程。
- 队伍成员与附近玩家分列。正式邀请固定所选 PlayerState，接受时重算真人与 AI 容量。增加拒绝邀请和单个自有同伴指令。
- 设置具有独立草稿与保存/取消/默认、音量、视角、UI 缩放、后端与键位。分辨率修改十五秒确认，退出页面或超时回退。键位只在输入上下文重叠时处理冲突。
- 正式 HUD 改为 UMG 生命/法力/体力、快捷技能、任务提示、交互和状态；Canvas 只保留显式开发诊断。

继续实施事项：正式资源化 WBP/图标、响应式细调、完整手柄输入资产、状态详情与冷却展示、自动后端实测配置、安全传送/离线队员边界，以及统一编译修复和实际交互验收。此单元不把所有菜单工作包标为已验收。
