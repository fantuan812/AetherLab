# v10 动作后端选择与真实过渡

自动模式由后台校验后的制品清单选择 CPU/Vulkan；自动 Vulkan 初始化失败可释放模型并重建 CPU，明确显示请求/实际后端和失败原因。显式 Vulkan 不偷偷改成成功报告。传统模式和资源缺失都有诊断，后端可启动并不表示目标硬件质量已验收。

组件随实际 Manny/Quinn 网格切换配置，异步资源回调带加载代数；旧资源不能覆盖新人物。闪避、受控动作时不接管姿态；锁定目标后使用战斗侧移风格。风格变化且已有实际消费上下文时，以带骨架哈希和固定 native revision 的四帧边界调用原生 transition inference；边界按真实胶囊朝向/位置转换，输出水平根位移仍不驱动角色。

作者脚本从真实烘焙 JSON 生成四帧边界资产并加入 Cook 标签。UE 动画导出拒绝未保存资源，并记录源 uasset 的 SHA256；风格侧文件保留源资源摘要及转换 JSON 摘要。

未编译、未测试；没有硬件性能、坐标校准或视觉质量验收结论。native 制品、动作与边界资产尚须统一生成和验证。
