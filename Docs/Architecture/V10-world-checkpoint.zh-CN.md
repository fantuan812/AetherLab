# v10 原生世界检查点

本单元未编译、未测试，仅进行源码审阅和 git diff --check。

- SQLite CompareExchangeWorld 仅更新已有 World/Main，校验精确期望版本、单次推进、4 MiB 上限；在同一写事务中解码世界并验证角色引用及不可变迁移来源。相同目标版本和负载重试返回 Replayed。
- FAetherWorldCheckpoint 在游戏线程从最新世界快照采集，异步提交；冲突最多重读/重采集三次，不给旧候选套新版本。只发布数据库确认的同一份负载。
- NativePersistence 激活后每十秒尝试保存已加载物理实体和天气，保留离线实体及最新交易事实。单个检查点在途，无过期采样队列。退出排空数据库写者，不将取消 Future 当作未提交证明。
- 登录完成通知移到队列处理之后，避免同步 Promise 续延退出/再次登录时破坏迭代。

此接口不授予玩家世界任意写权限；发奖励、领掉落等仍须原子跨聚合事务。自动物理保存不是区域卸载屏障：卸载仍需冻结待卸载实体并等待确认。GameMode 激活、完整遭遇/掉落恢复及区域切换仍待后续接线，不把新增底层代码当作 V10-04 验收通过。
