# v10 统一验证进行记录

本记录只记本轮实际执行结果，后续失败不得用历史通过覆盖。发布完成条件仍按 Implementation-v10 §18.2 执行。

| 项目 | 本轮实际状态 |
|---|---|
| UE 5.8 Editor Development / 非 Unity | 构建成功；已修复 JSON 键类型、头顺序、成员遮蔽、Pimpl 删除、模块链接依赖 |
| PowerShell / Python 作者脚本 | 语法检查通过；首次新建资源的可选加载和 LASTEXITCODE 误报已修复 |
| 受控动作 | 已生成 24 个官方动画派生序列及 DA_Actions；视觉、接触和网络验收待运行 |
| 原生 CPU 数据作者 | 校验锁定库/模型哈希并成功生成 34 骨 G1 描述及 6 段真实推理姿态 |
| 原生 Vulkan 制品 | 固定 native/GGML 提交构建成功；CPU、Vulkan 各 1 项 UE 真模型集成测试通过 |
| G1 mesh/IK/retarget 作者 | 资源生成成功，Manny/Quinn 双向 retarget、8 风格及边界已落盘 |
| 正式 UI / 输入 / 完整装备资源 | 作者链完成；正式内容命令行检查 0 错误、0 警告 |
| 规则回归 | 首轮 55/68；修复后 67/68；最后仓储用例针对复验通过，无遗留规则失败 |
| 实际菜单 / 视觉 | 五种分辨率交互复验通过；修复技能节点裁切、初始偏移、设置空选项、预览构图和格子布局；已检查实际截图 |
| Shipping Server | 实际调用 UBT 被安装引擎拒绝：Server targets are not currently supported from this engine distribution |
| 原生联机 | Dedicated/Listen 各正常及50ms单程延迟、3%丢包组合通过；包含四人、晚加入、重放、隐私、重连和重启 |
| Shipping Client | Win64 Shipping 编译成功；Cook/Package及包内运行继续验证 |
| 时长验收 | 已补生命周期探针，尚未运行，不作为已验收 |

Vulkan 构建依赖 LunarG 1.4.357.0，安装包 SHA256：
81f474711e9042f4cd22b31b2f7a8870db2e428b21586fb43dd80150be97310d。
下载签名有效，签名方 LunarG, Inc.；依照官方 copy_only 模式仅写入项目 Saved 缓存，未更改系统 PATH/注册表。

本地构建/作者日志位于 Saved 下，不提交日志或缓存。完成全部验收后再提交可移植结果索引和必要截图；当前没有宣称发布通过。

## 本轮资源与兼容修复

- 受控 24 动作、正式角色/G1 AnimBP、8 组风格与边界、输入映射、全部 UI Designer 蓝图/图标、双套装备及透明角色预览资源已生成。
- 蹲姿使用官方动作经 UE IK Retarget 反向转为 G1，再显式投影到固定骨长。每个采样检测投影前后全局关节位移，超过 5 cm 拒绝写出；本次最大 2.6774 / 2.6819 cm。此数值是转换误差，不能替代视觉动作验收。
- 旧模块迁移后的冻结反射指纹只归一模块包名，字段、类型、默认值仍按原冻结指纹校验；旧存档字节保持不变。
- 旧 Pawn 回执清除可转移字段，同时保留提交确定性及服务器世界恢复快照。
- 私人仓库允许原样保管锁定/本人绑定物品；共享转移、出售、丢弃仍执行移出限制。
- 按 v10 §13 奖励可分批领取更新回归：可容纳物品与金币结算、剩余保留原 RewardId、金币不能再次领取。v3 命令与完整 pre-WER1 DTO 兼容边界均有明确回归。

## 真实推理结果（不是性能矩阵）

CPU 4 次推理：63.012、83.267、81.845、81.650 ms。
Vulkan 4 次推理：3648.879、414.919、122.522、115.562 ms。
Vulkan 前两次包含冷启动/管线建立影响；不能排除这些数值后声称端到端 P95 达标。尚未执行渲染竞争、4 玩家并发和完整百分位采样。

## 2026-09-21 统一复验

- 最新完整规则：68/68 通过，报告 Rules-db85da3450a2444b87f8903491afc840。
- 原生联机报告：V10Native_beb3bec85458（Dedicated 正常）、V10Native_b1a298216c98（Dedicated 弱网）、V10Native_daa1489d1522（Listen 正常）、V10Native_db97a1c5de2c（Listen 弱网）。各弱网客户端检查 NetDriver 实际注入配置；双端各设置50ms延迟和3%丢包。它们是 Editor Development 进程，不能替代 Shipping 专服。
- 修复初始档案在资源恢复屏障完成前发出；探针等待 PlayerState 身份与安全传送提交，避免把尚未就绪的正确拒绝当成功路径。
- 菜单最终交互报告：720p 8ac9c9021c7e，1080p b3d1eb6301ab，1440p 51f4599f0187，4K 4fd7b849653b，21:9 d169273a4f96（目录前缀 AetherV10Menu_）。每次包括100次开关、换Pawn解绑重绑及七张实际 PNG；脚本读取PNG尺寸，不把命令行目标尺寸当实际结果。
- 内容复验：Content-2a14ca073ac64ef7881614e6ab322137，0错误、0警告。
- 旧档离线审计/导入/坏CRC拒绝：V10LegacyAudit/5c1bb3ad5ea3464fb54b752da9a89a9c。原样本字节不变。
- 普通事务强退恢复：V10Crash/f5bc565dd563462c9af1f7bb6dfd42b3；导入强退恢复：V10Crash/a3f04562984f4a3583a73494e09fd75e。五阶段全部符合预期退出码和持久状态，不代表断电/硬盘损坏测试。
- Win64 Shipping 客户端 UBT 构建成功（V10-shipping-client-build2.log）。之后新增仅开发探针与统计接口已通过 Editor 构建，后续包构建需要包含这些最新代码。

原生生命周期脚本 TestNativeSoak.ps1 默认3600秒，隔离新档，分三阶段运行传统/CPU/Vulkan、50次Pawn替换、50次跨区安全传送和至少100次菜单开关。它记录实际帧百分位和进程内存/对象/native计数，不代替完整主线、显存、纯净机器、Shipping或平台矩阵；目前未宣称运行通过。

## Shipping 客户端与生命周期短验

- 最新 Editor 构建成功；Win64 Shipping 全量 Cook、IoStore、Stage 与归档成功。首次 Zen Stage 连接失败保留在 package2 日志；使用引擎官方 -SkipZenStore 文件式 Cook 后成功，不修改受保护配置。
- 归档目录 Saved/Release/V10Candidate/Client/Windows。原生 stage.json 各文件逐一 SHA256 通过；真实运行观察到的 motionbricks/GGML DLL 路径均来自此包。
- Shipping Traditional：V10Package_d6453940440a41aeae45206d2d27eeff；CPU：V10Package_aad995d103454a9d968992837d41d449；Vulkan：V10Package_eaec415c81f44595b085bbfa00ff559c。三项均打开六页正式菜单、保存实际截图并验证关闭后输入/预览释放。CPU 首次脚本因进程模块缓存误判失败；实际报告有28次调用，修正采样后重新运行通过。
- 以上包 EXE SHA256：6757272318ee27032cffa05ccc9393b9418bc201fa43833feb24086c551bb898。构建基于6e83463加本工作单元的发布诊断/打包修改；旧报告的sourceCommit只记录基提交，此处明确记录工作树边界。之后脚本额外记录启动时未提交源文件列表。
- 10分钟短验 V10Soak_242b23909d18：600.02秒、50次Pawn替换、50次跨区安全传送、497次菜单开关；传统/CPU/Vulkan分别采样9695/9679/9695帧。P50约16.667ms，P95约29.706/29.769/29.776ms。此次与构建有重叠，不宣称正式60FPS性能达标。内存随后端切换变化，预览RenderTarget对象数随GC回落；资源平台需完整时长复核。
- Shipping诊断只读进度、通过正式菜单服务开页，必须指定独立UserDir及合成存档前缀；没有调试授予、战斗作弊或存储旁路。它验证包内可达资源，不代替真实输入、完整主线和纯净机器验证。

Shipping Server仍被安装版UE拒绝；Linux服务器工具链/构建亦未完成。完整主线、双人物动作质量、GPU显存及全部性能预算尚未验收。

## 实际动画图与重定向修复

检查旧包 World 截图发现人物仍处于参考姿势。根因是空 AnimBP 覆盖了仅对原生类有效的 GetCustomRootNode。Character 现在通过正式 AnimGraph 节点连接原生混合/动作/IK 图，G1 通过现有 GeneratedPose 节点连接输出；编辑节点使用 UncookedOnly 模块，避免未 Cook 的 -game 加载时丢失节点。

图生效后又发现 G1 参考骨盆位于原点，而 UE 按骨盆离地高度求重定向比例，导致人物根骨约75米高。现按 G1 足底校准参考高度，人物地面根由骨盆生成并固定 Z=0；反向输出的 G1 骨盆不再被地面根操作覆盖。重复作者按 UE 增量接口扣除已有偏移，避免累加。已重导出两组蹲姿、真实 mbstyle、烘焙片段和四帧边界。

实际检查：
- Editor 增量构建成功（animation-root-build10）。
- 正式传统 AnimBP：52根非参考姿势骨骼、58根随连续求值变化的骨骼，跳跃/落地/Montage通过（animation-root-check4）。
- CPU完整源到人物图：V10Pose_b8648b9b13464f499dd93e8aadaee342，源28根/人物20根非参考骨骼，混合权重1、人物根Z=0、骨盆约95.54cm；实际截图已检查。
- Vulkan最终复验：V10Pose_28ee6ab539b541f18e604e5d1ee73c5e；必须经过实际源姿态、人物姿态、根高度和稳定生成权重门槛。
- 内容验证：Content-791a73881a7e47f69e3a4f58b0d199b3通过，新增正式图连通性和G1参考高度检查。
- V10Soak_18cc95b7e95e 在发现旧动画缺陷后主动终止。之前的600秒生命周期检查只证明当时资源/进程行为，不证明正确动画图的最终性能；不得用其完成60分钟门槛。
- 旧 Shipping 包的启动/菜单/原生调用证据保留，但人物动画结论无效；发布包须重建复验。八风格双人物质量、完整玩法与最终性能仍在继续验证。

## 自定义风格真实推理修复

逐风格运行发现原先 crouch/crouch_idle 可被 loader 接受，但真实模型输出会向地下发散。作者转换现与锁定 native 的条件特征契约一致：只分离水平地面根轨迹，关节保留 Y-up 离地高度；四个 dummy 手/趾末端的全局条件旋转固定为单位矩阵，FK 位置保留。

TestMotionStyles.py 使用真实 CPU 模型，对 idle/crouch/crouch_idle 各三个种子连续生成六秒，并执行原生 idle → 作者风格 → 真实生成的往返检查。全部通过有限变换、四元数范数和骨盆高度门槛，报告 Saved/Automation/V10-style-regression/result.json。这项检查不会修整输出，也不替代视觉质量审批。

实际 Editor 16 组 Manny/Quinn 动画图检查 V10Quality_bce957e43edb、V10Quality_a119d870e41c 已确认每种风格实际更新且不再出现骨盆发散。截图进一步揭示蹲起后人物 Mesh 默认偏移错误；该问题正在另一个工作单元修复，因此此处仍不将双人物视觉、接地和发布包标记通过。

## 蹲起 Mesh 偏移回归

UE 的蹲起回调使用类默认 Mesh 偏移，而本项目人物定义在运行时设置偏移；现按实际站立胶囊高度恢复偏移，并先同步父类直接修改后的世界变换缓存。回归同时断言相对位置和世界位置，Rules-ced281a0f9f64c4fa7b309ba27263788 通过。Editor 增量构建通过（V10-crouch-mesh-build2.log）。

真实 CPU 双人物16风格检查 V10Quality_8a45883b8767 通过，人物 Mesh 恢复至地面，足骨离地最大约12–15.24cm（目标包含12cm足骨偏移）。这些测量不代表滑脚或生成蹲姿幅度已经验收；生成蹲姿视觉仍需改进，完整质量和发布包继续验证。

## 解剖关节、接地和军械服务统一修复

- 蹲姿作者以真实原始足部轨迹为约束求解双腿 IK；G1 的左右坐标、人物网格前向和反向导出统一。六关节机器人腿分为髋/膝/踝三段映射，重复作者保留已校准的骨盆参考高度与朝向。
- 最终脚部 IK 从本帧未经 IK 的足部姿态计算地形修正，保留摆腿高度和水平步幅，避免上一帧落地 socket 形成反馈。源网格更换采用唯一 UObject 名称，消除覆盖旧组件的同步清理警告。
- 最新真实 CPU 风格/往返回归 V10-style-facing-regression 通过。CPU 图检查 V10Quality_9cdc9ad49daa 通过，但在脚步修正前；脚步修正后的 Vulkan 双人物16风格 V10Quality_18246032b36b 通过。视觉、完整动作接触和性能不因图检查通过而自动验收。
- 手柄真实 Enhanced Input 与 Slate 焦点检查 AetherV10Menu_ba49be82a27c 通过：View/Menu 键、六页肩键循环及反向循环、返回游戏；含换 Pawn 与100次菜单开关，实际1920×1080截图。
- 实际主线发现军械商缺少世界实例，且正式交易错误依赖旧商店目录。现加入军械商实例（允许旧存档缺少新增对象），交易权限、提示、NPC外观查询 v10 商店目录。完整探针已实际完成购买铁剑/胸甲、穿戴、铜戒技能授予/撤销和买卖金币守恒；后续药剂/仓储/修道院尚在运行，不能将全主线记为通过。
- Editor 增量编译 V10-market-regression-build 成功；全套轻量规则68/68通过，Rules-0fa3795913114f9fa4af5db69a9b4514，包含新增世界商店可达性及 v10-only 军械交易回归。
- 地图导航池容量已保存为16384；主线往返日志未再出现此前256瓦片池耗尽警告。
- 长时探针新增真实命令、ASC/资源预留/队列计数、桥接GT/计划端到端/Slate总成本和进程WDDM显存采样。尚未执行最终一小时，不宣称性能预算通过。
